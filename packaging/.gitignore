# Build outputs — regenerated by packaging/build.sh, never committed.
build/
dist/

# Downloaded/derived rclone binary — packaging/fetch_rclone.sh's output.
# Pinned version + sha256 live in that script, not here.
vendor/
