<!--
  The compiler requires a launcher icon on every app type (jumpfield hit the
  same wall on its first real build, FIRST_COMPILE.md #8). 60x60 because
  that is epix2's "Launcher Icon Size" in the SDK device reference
  (doc/docs/Device_Reference/epix2.html). Generated by a throwaway zlib
  script, no image editor — a battery outline with a fill bar, which is
  literally what this app is.
-->
<drawables>
    <bitmap id="LauncherIcon" filename="launcher_icon.png" />
</drawables>
